import Confetti from "react-confetti";

const App = () => {
    return (
        <div className="flex items-center justify-center w-screen h-screen">
            <Confetti />
        </div>
    );
};

export default App;
